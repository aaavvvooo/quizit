name: CI

on:
  push:
  pull_request:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repo sanity
        run: |
          test -d backend
          test -d frontend
          test -d infra
          test -d docs
          echo "Structure OK"
